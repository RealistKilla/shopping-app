/**
 * =============================================================================
 * shared/catalog/constants.ts — Shared Catalog Constants
 * =============================================================================
 */


/**
 * A generic subtle blurhash to use as a placeholder while network images load.
 * This is shared across all features (listing, detail, cart) that render 
 * product images to ensure a consistent skeleton state.
 */
export const PRODUCT_IMAGE_BLURHASH = "L9AB*A%MtRM|00RjV@of00bI?HWB";

